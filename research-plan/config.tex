%% ==========================================================================
%%  CONFIGURATION — Colours, Fonts, Margins, Hyperlinks
%%  -----------------------------------------------------------------------
%%  Edit this file to quickly retheme or adjust the layout.
%%  Everything here is a knob you can turn without touching content.
%% ==========================================================================

% ---------- COLOUR PALETTE ----------
\definecolor{primary}{RGB}{20, 50, 90}        % dark navy — headings & name
\definecolor{accent}{RGB}{42, 98, 154}        % steel blue — links
\definecolor{subtle}{RGB}{108, 117, 125}      % warm grey — secondary text
\definecolor{divider}{RGB}{200, 208, 216}     % light grey — section rules

% ---------- MARGINS ----------
\geometry{
    a4paper,
    top    = 1in,
    bottom = 1in,
    left   = 1in,
    right  = 1in,
}

% ---------- FONTS ----------
% Charter — a highly legible serif, standard for academic documents.
% Swap the package below to change the whole look:
%   \usepackage{palatino}    — classic, elegant
%   \usepackage{times}       — compact, traditional
%   \usepackage{libertine}   — modern, open-source
\usepackage{charter}

% ---------- FONT SIZES (easy to tweak) ----------
\newcommand{\namesize}{\fontsize{14}{17}\selectfont}
\newcommand{\titlesize}{\fontsize{16}{20}\selectfont}
\newcommand{\headerinfosize}{\fontsize{10}{13}\selectfont}
\newcommand{\sectionheadsize}{\fontsize{12}{15}\selectfont}
\newcommand{\bodysize}{\fontsize{11}{14}\selectfont}
\newcommand{\smallsize}{\fontsize{10}{12}\selectfont}

% ---------- HYPERLINKS ----------
\hypersetup{
    colorlinks  = true,
    urlcolor    = accent,
    linkcolor   = accent,
    pdfauthor   = {Florian Braun},
    pdftitle    = {Research Plan},
    pdfsubject  = {Research Plan},
}

% ---------- SECTION HEADING STYLE ----------
\titleformat{\section}
    {\sectionheadsize\bfseries\color{primary}}
    {\thesection.}
    {0.5em}
    {}
    [\vspace{-4pt}{\color{divider}\rule{\linewidth}{0.6pt}}]

\titlespacing*{\section}{0pt}{14pt}{6pt}

% ---------- GLOBAL SPACING ----------
\setlength{\parindent}{0pt}
\setlength{\parskip}{8pt}

% ---------- PAGE FOOTER ----------
% Right-aligned page number + plan identifier
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[L]{{\smallsize\color{subtle}Research Plan --- Florian Braun}}
\fancyfoot[R]{{\smallsize\color{subtle}Page \thepage}}
