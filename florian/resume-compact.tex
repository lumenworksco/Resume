%% ==========================================================================
%%  Compact Resume — One-page highlights version
%%  -----------------------------------------------------------------------
%%  Compile with:  pdflatex resume-compact.tex   (run twice for hyperlinks)
%%
%%  Uses the same config & commands as the full resume, then overrides
%%  margins and spacing for a tighter one-page layout.
%% ==========================================================================

\documentclass[10pt, a4paper]{article}

% ---------- CORE PACKAGES ----------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{tabularx}
\usepackage{multicol}
\usepackage{tikz}
\usepackage{fontawesome5}

% ---------- LOAD SHARED PROJECT FILES ----------
\input{config}
\input{commands}

% ---------- COMPACT OVERRIDES ----------
% Tighter margins
\geometry{
    a4paper,
    top    = 0.3in,
    bottom = 0.3in,
    left   = 0.4in,
    right  = 0.4in,
}

% Slightly smaller body font
\renewcommand{\bodysize}{\fontsize{8.5}{11}\selectfont}
\renewcommand{\smallsize}{\fontsize{7.5}{9.5}\selectfont}
\renewcommand{\namesize}{\fontsize{22}{26}\selectfont}
\renewcommand{\headlinesize}{\fontsize{9}{11}\selectfont}
\renewcommand{\contactsize}{\fontsize{8}{10}\selectfont}

% Tighter section spacing
\titlespacing*{\section}{0pt}{5pt}{2pt}

% ---------- SANS-SERIF DEFAULT ----------
\renewcommand{\familydefault}{\sfdefault}

% ==========================================================================
%                             DOCUMENT
% ==========================================================================
\begin{document}

\bodysize

\input{content-compact}

\end{document}
